{
  "name": "firebase-review-app",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "start": "concurrently \"npm --prefix client start\" \"npm --prefix server start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}